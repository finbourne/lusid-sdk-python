# AssetLeg

The underlying instrument representing one side of the TRS and its pay-receive direction.
## Properties
Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------
**asset** | [**LusidInstrument**](LusidInstrument.md) |  | 
**asset_swap_structure** | **str** | The relationship between the funding leg&#39;s notional and the asset&#39;s principal, for a bond-family Asset.  Par (and absent) requires the funding leg&#39;s notional to equal the asset&#39;s principal; Proceeds allows the  funding leg&#39;s notional to be the market proceeds of the asset instead, sizing the two legs on independent  bases. Optional and absent by default; absence is distinct from Par so that behaviour keyed to the field  being supplied fires only when it is.                Supported string (enumeration) values are: [Par, Proceeds]. | [optional] 
**income_policy** | **str** | Controls what happens to income (coupons, dividends) generated by the Asset.  Defaults to PassThrough, preserving today&#39;s behaviour of emitting income as flows on their natural payment dates.                Supported string (enumeration) values are: [PassThrough, AccrueToReset, Reinvest]. | [optional] 
**initial_price** | **float** | The reference price of a single unit of the Asset at inception, used as the starting point (P_0) for  price-return resets. Optional and absent by default, preserving today&#39;s behaviour for TRS definitions  that predate price-return semantics. | [optional] 
**pay_receive** | **str** | Either Pay or Receive stating direction of the asset in the swap.                Supported string (enumeration) values are: [Pay, Receive]. | 
**reset_schedule** | [**ResetSchedule**](ResetSchedule.md) |  | [optional] 
## Example

```python
from lusid.models.asset_leg import AssetLeg
from typing import List, Dict, Optional, Any, Union, TYPE_CHECKING
from typing_extensions import Annotated
from pydantic.v1 import BaseModel, StrictStr, StrictInt, StrictBool, StrictFloat, StrictBytes, Field, validator, ValidationError, conlist, constr
from datetime import datetime

asset: LusidInstrument
asset_swap_structure: Optional[StrictStr] = "example_asset_swap_structure"
income_policy: Optional[StrictStr] = "example_income_policy"
initial_price: Optional[Union[StrictFloat, StrictInt]] = # Replace with your value
pay_receive: StrictStr = "example_pay_receive"
reset_schedule: Optional[ResetSchedule] = # Replace with your value
asset_leg_instance = AssetLeg(asset=asset, asset_swap_structure=asset_swap_structure, income_policy=income_policy, initial_price=initial_price, pay_receive=pay_receive, reset_schedule=reset_schedule)

```

[Back to Model list](../README.md#documentation-for-models) &#8226; [Back to API list](../README.md#documentation-for-api-endpoints) &#8226; [Back to README](../README.md)

