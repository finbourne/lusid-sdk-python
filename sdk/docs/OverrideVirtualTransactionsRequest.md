# OverrideVirtualTransactionsRequest

Upserts overrides and suppressions of virtual transactions generated by a single instrument event.  Overrides are keyed by the virtual transaction id being overridden; suppressions are the list of virtual  transaction ids being suppressed. A virtual transaction id must not appear in both. This is a full  replace of the previously stored overrides and suppressions for the targeted instrument event - any  previously stored entry not present in this request is removed.
## Properties
Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------
**overrides** | **Dict[str, Optional[List[OverrideDefinitionRequest]]]** | A map of virtual transaction id to one or more replacement transactions. A virtual transaction id cannot also appear in Suppressions. | [optional] 
**suppressions** | **List[str]** | The virtual transaction ids to suppress. A virtual transaction id cannot also appear in Overrides. | [optional] 
## Example

```python
from lusid.models.override_virtual_transactions_request import OverrideVirtualTransactionsRequest
from typing import List, Dict, Optional, Any, Union, TYPE_CHECKING
from typing_extensions import Annotated
from pydantic.v1 import BaseModel, StrictStr, StrictInt, StrictBool, StrictFloat, StrictBytes, Field, validator, ValidationError, conlist, constr
from datetime import datetime

overrides: Optional[Dict[str, Optional[List[OverrideDefinitionRequest]]]] = # Replace with your value
suppressions: Optional[List[StrictStr]] = # Replace with your value
override_virtual_transactions_request_instance = OverrideVirtualTransactionsRequest(overrides=overrides, suppressions=suppressions)

```

[Back to Model list](../README.md#documentation-for-models) &#8226; [Back to API list](../README.md#documentation-for-api-endpoints) &#8226; [Back to README](../README.md)

